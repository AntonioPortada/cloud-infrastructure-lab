server {

  listen 80;
  server_name localhost;

  #location / {
   # root /usr/share/nginx/html;
  #}

  location / {
    proxy_pass http://web:80/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    #proxy_redirect http://web/ http://localhost/;
  }

}
